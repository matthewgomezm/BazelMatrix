# Bazel Matrix
- Using Bazel to build and run a one-file C++ project.
## How to use
- Install Bazel. 
- Then, clone repo and cd/stage/
- Makefile will be ready, so simply run _make_ to build and run. 

